import React from "react";
import styled, { StyledFunction } from "styled-components";
import "./App.css";

const App: React.FC = () => {
  return (
    <div className="App">
    </div>
  );
};

export default App;
